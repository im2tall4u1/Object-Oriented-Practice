class Dog:
    species = 'mammal'
    def __init__(self, name, age):
        self.name = name
        self.age = age
fred = Dog('Fred', 5)
barney = Dog('Barney', 6)

print('{} is {} and {} is {}.'.format(
    fred.name, fred.age, barney.name, barney.age))


